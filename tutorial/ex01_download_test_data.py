"""
Ex 01 — Download test data
Data already lives at FAST/data/; this just confirms the path is set.
"""
from _config import fast, DATA

print(f"Test data path: {DATA}")
print("Data already present — no download needed.")
